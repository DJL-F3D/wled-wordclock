; ============================================================
; platformio_override.ini — Word Clock custom build environments
;
; This file is copied into the WLED source root by build.yml.
; WLED picks it up automatically alongside its own platformio.ini.
;
; DATA_PINS — change the number to match your wiring:
;   ESP32-C3 default : 8  (GPIO8 on SuperMini)
;   ESP8266  default : 2  (GPIO2 = D4 on Wemos D1 Mini)
; ============================================================

[platformio]
default_envs = wordclock_esp32c3, wordclock_esp8266

; ── ESP32-C3 SuperMini ───────────────────────────────────────────────────────
; The SuperMini uses the ESP32-C3's built-in native USB port (GPIO18/19)
; rather than a separate USB-UART chip like the DevKitM-1.
;
; WLED's base esp32c3 environment inherits ARDUINO_USB_CDC_ON_BOOT=1,
; which tells the chip to wait for a USB CDC serial handshake before
; continuing boot.  On the SuperMini running standalone that handshake
; never arrives, the watchdog fires, and the board reboots endlessly.
;
; Fix: strip ARDUINO_USB_CDC_ON_BOOT=1 via build_unflags and replace
; it with 0 so the chip boots normally without needing a serial host.
[env:wordclock_esp32c3]
extends           = esp32c3
platform          = ${esp32c3.platform}
platform_packages = ${esp32c3.platform_packages}
board             = esp32-c3-devkitm-1
build_unflags     = ${common.build_unflags}
  -D ARDUINO_USB_CDC_ON_BOOT=1
build_flags       = ${common.build_flags} ${esp32c3.build_flags}
  -D ARDUINO_USB_CDC_ON_BOOT=0
  -D USERMOD_ID_WORDCLOCK=99
  -D DATA_PINS=8

; ── ESP8266 (Wemos D1 Mini / NodeMCU v2) ─────────────────────────────────────
[env:wordclock_esp8266]
board             = d1_mini
platform          = ${common.platform_wled_default}
platform_packages = ${common.platform_packages}
board_build.ldscript = ${common.ldscript_4m1m}
build_unflags     = ${common.build_unflags}
build_flags       = ${common.build_flags} ${esp8266.build_flags}
  -D USERMOD_ID_WORDCLOCK=99
  -D DATA_PINS=2
lib_deps          = ${esp8266.lib_deps}
